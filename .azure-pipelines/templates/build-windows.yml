parameters:
  - name: extraflags
    default: ''

steps:
  - task: UsePythonVersion@0
    inputs:
      versionSpec: '3.x'
  - script: |
      choco install -y mingw
    displayName: 'Install Prerequisites'
  - powershell: |
      $ErrorActionPreference = 'Stop'
      mkdir build
      cd build
      cmake ../ -G "MinGW Makefiles" -A "x64"  ${{ parameters.extraflags }}
      cmake --build . --config Release
      cmake --install .
    displayName: 'Build'
